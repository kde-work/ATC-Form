<IfModule mod_rewrite.c>
    <IfModule mod_negotiation.c>
        Options -MultiViews -Indexes
    </IfModule>

    RewriteEngine On

    # Handle Authorization Header
    RewriteCond %{HTTP:Authorization} .
    RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]

    # Handle X-XSRF-Token Header
    RewriteCond %{HTTP:x-xsrf-token} .
    RewriteRule .* - [E=HTTP_X_XSRF_TOKEN:%{HTTP:X-XSRF-Token}]

    # Redirect Trailing Slashes If Not A Folder...
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_URI} (.+)/$
    RewriteRule ^ %1 [L,R=301]

    # Корень сайта: Angular, не Laravel index.php (DirectoryIndex по умолчанию берёт index.php первым).
    DirectoryIndex index.html index.php

    # API -> Laravel (/api/... или /public/api/... после rewrite из корневого .htaccess)
    RewriteCond %{REQUEST_URI} /api/
    RewriteRule ^ index.php [L]

    # Существующие файлы (Angular assets, images, favicon)
    RewriteCond %{REQUEST_FILENAME} -f
    RewriteRule ^ - [L]

    # Angular SPA fallback
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.html [L]
</IfModule>
